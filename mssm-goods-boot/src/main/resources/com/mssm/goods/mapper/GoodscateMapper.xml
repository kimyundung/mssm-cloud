<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mssm.goods.mapper.GoodscateMapper">
    <!--添加-->
    <insert id="add" parameterType="com.mssm.common.domain.Goodscate">
        insert into goodscate (goods_id, cate_id, cate_name)
        values (#{goodsId},#{cateId},#{cateName})
    </insert>
</mapper>